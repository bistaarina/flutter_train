import 'package:flutter/material.dart';

class ProductDesScreen extends StatelessWidget {
  const ProductDesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}